header.header
  .container-fluid
    .row.align-items-center
      .col-3
        .inner-logo
          a(href=`${prefixAdmin}/dashboard`) ADMIN
      .col-9
        div(class='text-right')
          a(href=`${prefixAdmin}/auth/logout`, class='btn btn-danger') Đăng xuất
          //- a(href=`${prefixAdmin}/dashboard`, class='btn btn-secondary') Trang quản trị
          //- a(href=`${prefixAdmin}/`, class='btn btn-info') Trang chủ
          //- a(href=`${prefixAdmin}/auth/login`, class='btn btn-success') Đăng nhập
